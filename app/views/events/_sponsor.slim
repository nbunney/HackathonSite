/ We misspell "sponsor" intentionally to avoid triggering AdBlock
div class="col-sm-#{12 / tier.columns} sponser"
  =image_tag company.logo, class: 'sponser-logo', alt: company.name
  p=company.description
